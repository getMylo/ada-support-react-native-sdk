apply plugin: 'com.android.library'

android {
	compileSdkVersion 31
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 30
    }
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
}
